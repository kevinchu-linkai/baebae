Drop photos here (e.g. `01.jpg`, `02.jpg`, ...), then in `src/pages/Gallery.jsx`
set each placeholder's `src: null` to `src: '/gallery/01.jpg'` and update the
caption. The `span` field controls each tile's footprint in the grid — keep a
mix of tall/wide tiles so the layout stays asymmetric.
